<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Center the existing PNG and give it some padding so it doesn't get cut off by masks -->
    <item
        android:drawable="@drawable/socialone"
        android:gravity="center"
        android:width="72dp"
        android:height="72dp"
        android:top="10dp"
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp" />
</layer-list>